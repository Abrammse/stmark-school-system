    <section id="multiple-column-form">
        <div class="row match-height">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h4 class="card-title" style="font-family:'Cairo',sans-serif;">البيانات</h4>
                    </div>
                    <div class="card-content">
                        <div class="card-body">
                            {% if action is None %}
                            <form class="form" action="?action=get_student" method="post" >
                                {% csrf_token %}
                                <div class="row">

    <div class="form-group has-icon-left">
        <label for="course" style="font-family:'Cairo',sans-serif;"> المادة </label>
        <div class="position-relative">
            <select    class="form-select"  name="subject"  >
                                {% for course in subjects %}

                <option value="{{ course.id }}" >  {{ course.subject_name }}</option>
                             {% endfor %}

            </select>
        </div>
    </div>
<div class="form-group has-icon-left">
    <label for="course" style="font-family:'Cairo',sans-serif;">الترم</label>
    <div class="position-relative">
        <select class="form-select" name="session">
            {% for staff in session_years %}
                <option value="{{ staff.id }}" >  {{ staff.Term }}  من   {{ staff.session_start_year }} حتى  {{ staff.session_end_year }}</option>
                {% endfor %}
        </select>
    </div>
</div>



                                        <div class="col-12 d-flex justify-content-end">

                                        <button type="submit" class="btn btn-primary me-1 mb-1" style="font-family:'Cairo',sans-serif;"   > اضافة  ترم</button>
                                        <button type="reset" class="btn btn-light-secondary me-1 mb-1"   style="font-family:'Cairo',sans-serif;" >افراغ الحقول</button>
                                    </div>
                                </div>
                            </form>
                        {% else %}
                        <form class="form" action="{% url 'staff_take_attendance_save' %}" method="post" >
                                {% csrf_token %}
                                <div class="row">

   <div class="form-group has-icon-left">
        <label for="course" style="font-family:'Cairo',sans-serif;"> المادة </label>
        <div class="position-relative">
            <select   style="font-family:'Cairo',sans-serif;"  class="form-select"  name="subject"  >

                <option value="{{ subject_obj.id }}" style="font-family:'Cairo',sans-serif;"  >  {{ subject_obj }}</option>

            </select>
        </div>
    </div>
<div class="form-group has-icon-left">
    <label for="course" style="font-family:'Cairo',sans-serif;">الترم</label>
    <div class="position-relative">
        <select class="form-select" style="font-family:'Cairo',sans-serif;" name="session">
                <option value="{{ session_obj.id }}" style="font-family:'Cairo',sans-serif;" >  {{ session_obj }}    </option>
        </select>
    </div>
</div>
                                    <div class=" col-12">
                                        <div class="form-group  has-icon-left">
                                            <label for="email-id-column" style="font-family:'Cairo',sans-serif;">   اختر التاريخ </label>
                                            <div  class="position-relative">
                                            <input type="date"  name="attendance_date"  style="font-family:'Cairo',sans-serif;" class="form-control"  placeholder=" ادخل اسم   المادة  ">
                                             <div class="form-control-icon">
<i class="bi bi-calendar2-check-fill"></i>

                                             </div>

                                            </div>
                                        </div>
                                    </div>

                                    <div class="   col-12">
                                        <div class="table-responsive datatable-minimal">
                    <table class="table" id="table2">
                        <thead>
        <tr>
                      <th  style="font-family:'Cairo',sans-serif;">اختر </th>
                      <th  style="font-family:'Cairo',sans-serif;  text-align:center;">اسم الطالب </th>


        </tr>
    </thead>
    <tbody>
        {% for subject in students %}
                    <tr>
                        <td><input  type="checkbox" value="{{subject.id}}" class="form-check-input form-check-primary" name="student_id" id="customColorCheck1"></td>
                      <td style=" text-align:center; font-family:'Cairo',sans-serif; ">{{ subject.name }}</td>
                    </tr>
                  {% endfor %}
    </tbody>
                    </table>
                </div>

                                    </div>


                                        <div class="col-12 d-flex justify-content-end">

                                        <button type="submit" class="btn btn-primary me-1 mb-1" style="font-family:'Cairo',sans-serif;"   > تسجيل   ترم</button>
                                    </div>
                                </div>

                        {% endif %}

                        </form>
                    </div>

                </div>
            </div>
        </div>
        </div>
    </section>
